:root {
  /* 🎯 Red Felt Table Theme */
  --felt-dark: #4b0505;
  --felt-mid: #7a0f0f;
  --felt-light: #b71c1c;
  --wood: #5a2a0c;
  --card-w: 80px;
  --card-h: 115px;
  --accent: #ffcb74;
  --glass: rgba(255, 255, 255, 0.04);
  --shadow: rgba(0, 0, 0, 0.6);
}

/* ===== BASE ===== */
body {
  margin: 0;
  font-family: "Poppins", system-ui, Arial, sans-serif;
  background: radial-gradient(circle at 20% 20%, #630202, #410101 80%);
  color: #fff;
  display: flex;
  justify-content: center;
  padding: 14px;
}

#app {
  width: 1000px;
  max-width: 96%;
}
.brand {
  display: flex;
  align-items: center;
  gap: 10px;
}



.title {
  font-weight: 800;
  font-size: 20px;
  color: var(--accent);
  text-shadow: 0 2px 8px rgba(0,0,0,0.6);
}

/* ===== CONTROLS ===== */
.controls {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  align-items: center;
}

select {
  background: rgba(0,0,0,0.25);
  color: #fff;
  padding: 6px 10px;
  border-radius: 6px;
  border: 1px solid rgba(255,255,255,0.08);
}

.btn {
  padding: 7px 10px;
  border-radius: 8px;
  background: var(--glass);
  border: 1px solid rgba(255,255,255,0.05);
  cursor: pointer;
  font-weight: 600;
  color: #fff;
  transition: all 0.25s ease;
}

.btn:hover {
  transform: translateY(-2px);
  background: rgba(255,255,255,0.08);
}

.btn.primary {
  background: var(--accent);
  color: #1a1a1a;
  box-shadow: 0 6px 16px rgba(255,215,100,0.3);
}

.btn.primary:hover {
  transform: scale(1.05);
  box-shadow: 0 0 15px rgba(255,215,100,0.6);
}

.btn.danger {
  background: #b22;
}

/* ===== LAYOUT ===== */
.main-wrap {
  display: flex;
  gap: 14px;
  align-items: flex-start;
}
/* ===== CENTER AREA ===== */
.center-area {
  width: 420px;
  height: 280px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  z-index: 2;
}

.played-area {
  display: flex;
  gap: 10px;
  justify-content: center;
  align-items: center;
  width: 100%;
  min-height: 120px;
}

.played-slot {
  width: 86px;
  height: 122px;
  border-radius: 8px;
  background: rgba(255,255,255,0.06);
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.25s;
}

.played-slot img {
  width: 100%;
  height: 100%;
  border-radius: 6px;
  object-fit: cover;
}

/* ===== ROUND INFO ===== */
.round-info {
  position: absolute;
  top: 1px;                 /* move it to top of table */
  left: 90%;
  transform: translateX(-50%);
  font-weight: 900;
  font-size: 22px;
  padding: 8px 18px;
  border-radius: 20px;
  background: rgba(0,0,0,0.35);
  color: var(--accent);
  box-shadow: 0 6px 14px rgba(0,0,0,0.5);
  z-index: 100;
}

/* ================= PLAYED CARDS ================= */
.played-slot {
  width: 86px;
  height: 122px;
  border-radius: 8px;
  background-color: #e58804; /* card background */
  display: flex;
  flex-direction: column;    /* stack name above card */
  align-items: center;
  justify-content: flex-start; /* start from top for name */
  gap: 4px;                  /* spacing between name and card image */
  transition: all 0.25s;
  box-shadow: 0 6px 16px rgba(0,0,0,0.5);
  position: relative;
  padding-top: 20px;         /* more space for bigger name */
}

/* Player name above card */
.played-slot .player-name {
  position: absolute;
  top: 2px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 18px;           /* bigger name */
  font-weight: 900;
  color: #ff0000;            /* red color */
  text-shadow: 0 1px 3px rgba(0,0,0,0.5);
  pointer-events: none;      /* name doesn’t block card clicks */
}

/* Card image inside played slot */
.played-slot img {
  width: 80px;
  height: 115px;
  object-fit: cover;
  border-radius: 6px;
  background-color: #f50909;
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

/* Gap between multiple played cards on table */
.played-area {
  display: flex;
  gap: 14px; /* spacing between multiple cards */
  justify-content: center;
  align-items: center;
  width: 100%;
  min-height: 120px;
}


/* Card image inside played slot */
.played-slot img {
  width: 80px;
  height: 115px;
  object-fit: cover;
  border-radius: 6px;
  background-color: #ffffff; /* force white */
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

/* Gap between multiple played cards on table */
.played-area {
  display: flex;
  gap: 14px; /* increase gap between cards */
  justify-content: center;
  align-items: center;
  width: 100%;
  min-height: 120px;
}


/* ===== FIXED POSITIONS ===== */
.player-slot {
  position: relative;
  text-align: center;
}

.player-slot .player-icon {
  width: 70px;
  height: 70px;
  border-radius: 50%;
  margin-bottom: 4px;
  border: 2px solid #fff;
  background-color: #ccc;
}

/* Top player */
.player-slot.top {
  top: -40%;
  left: 60%;
  transform: translateX(-50%);
}
.player-slot.bottom {
  bottom: -40%;
  left: 28%;
  transform: translateX(-50%);
}
.player-slot.left {
  top: 10%;
  left: -8%;
  transform: translateY(-50%);
}
.player-slot.right {
  top: 5%;
  right: -70%;
  transform: translateY(-50%);
}



.bottom-area {
  position: absolute;
  bottom: 20px;
  left: 50%;
  transform: translateX(-50%);
  width: 92%;
  display: flex;
  justify-content: center;
  z-index: 3;
}

/* Player hand general styles */
.player-hand {
  pointer-events: auto;
  align-items: center;
  justify-content: center;
  flex-wrap: nowrap;
  padding: 4px;
  overflow-x: auto;
  z-index: 999;
}

/* ================= CARDS ================= */
/* ================= CARDS ================= */
.card-img {
  width: var(--card-w);
  height: var(--card-h);
  border-radius: 8px;
  background-color: #ffffff; /* solid white background */
  box-shadow: 0 8px 18px rgba(0,0,0,0.6);
  object-fit: cover;
  cursor: pointer;
  transition: transform 0.18s ease, box-shadow 0.18s ease, filter 0.18s ease;
  z-index: 1000;
}

/* flying / animated card */
.anim-card {
  pointer-events: none;
  position: absolute;
  width: var(--card-w);
  height: var(--card-h);
  border-radius: 8px;
  background-color: #ffffff; /* solid white during flight */
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 10px 20px rgba(0,0,0,0.5);
  transition: transform 0.4s ease, opacity 0.4s ease;
  z-index: 2000;
  opacity: 1;
}

.anim-card img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 8px;
  background-color: #ffffff; /* force white behind image */
  display: block;
}

/* played cards on table */
.played-slot {
  width: 86px;
  height: 122px;
  border-radius: 8px;
  background-color: #ffffff; /* solid white */
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.25s;
  box-shadow: 0 6px 16px rgba(0,0,0,0.5);
}

.played-slot img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 6px;
  background-color: #ffffff; /* force white */
}




/* ===== ANIMATIONS ===== */
.fly-to-center {
  animation: flyToCenter .45s ease forwards;
}

@keyframes flyToCenter {
  0% { transform: none; }
  70% { transform: translateY(-10px) scale(1.02); }
  100% { transform: translateY(-40px) scale(0.9); }
}

/* ===== SIDE PANEL ===== */
.panel {
  width: 300px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.card {
  background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0.08));
  border-radius: 10px;
  padding: 12px;
  box-shadow: 0 10px 30px rgba(0,0,0,0.55);
}

.card h3 {
  margin: 0 0 8px 0;
  font-size: 16px;
  color: var(--accent);
}

#log {
  max-height: 300px;
  overflow: auto;
  font-size: 13px;
}

/* ===== FOOTER ===== */
.footer {
  margin-top: 12px;
  font-size: 13px;
  opacity: 0.9;
  text-align: center;
}

/* ===== RESPONSIVE FIX ===== */
@media (max-width: 900px) {
  #app { width: 94%; }
  .table { height: 500px; }
  .panel { width: 100%; }
  .main-wrap { flex-direction: column-reverse; align-items: center; }
  .player-slot.left, .player-slot.right {
    display: none;
  }
  .player-slot.top {
    top: 5%;
  }
}
/* popup styling */
.popup {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
  animation: fadeIn .3s ease;
}
.popup-content {
  background: #fff;
  color: #111;
  padding: 24px 32px;
  border-radius: 16px;
  text-align: center;
  box-shadow: 0 10px 40px rgba(0,0,0,0.4);
}
@keyframes fadeIn { from{opacity:0} to{opacity:1} }

/* smooth fly-to-center already exists */
/* animated temp card */
.anim-card {
  pointer-events: none;
  border-radius: 8px;
  box-shadow: 0 10px 20px rgba(0,0,0,0.5);
  transition: none; /* no transition delay for instant moves */
  opacity: 0;
}
.player-slot.active {
  box-shadow: 0 0 12px 4px #ffcb74 inset;
  transform: scale(1.05);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.turn-indicator {
  pointer-events: none;
}


.player-hand {
  z-index: 999;
  pointer-events: auto;
}



.popup {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0);
  background: rgba(0,0,0,0.85);
  color: #fff;
  padding: 18px 28px;
  border-radius: 12px;
  box-shadow: 0 6px 18px rgba(0,0,0,0.35);
  z-index: 9999;
  opacity: 0;
  transition: transform 0.4s cubic-bezier(0.68,-0.55,0.27,1.55), opacity 0.3s;
  text-align: center;
}

.popup.show {
  transform: translate(-50%, -50%) scale(1);
  opacity: 1;
}

.popup-content h2 {
  margin: 0 0 8px 0;
  font-size: 20px;
}

.popup-content p {
  margin: 0;
  font-size: 16px;
}
